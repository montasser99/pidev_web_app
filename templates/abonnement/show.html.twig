{% extends 'base-back.html.twig' %}

{# {% block title %}Abonnement{% endblock %} #}

{% block contenu %}
    
      <div class="pagetitle">
      <h1>Abonnement</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="{{ path('app_abonnement_index') }}">back to list</a></li>
          <li class="breadcrumb-item"><a href="{{ path('app_abonnement_edit', {'id': abonnement.id}) }}">edit</a></li>
      
        </ol>
      </nav>
    </div><!-- End Page Title -->
    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonnement.id }}</td>
            </tr>
            <tr>
                <th>IdU</th>
                <td>{{ abonnement.idU }}</td>
            </tr>
            <tr>
                <th>MoyenTransportA</th>
                <td>{{ abonnement.moyenTransportA }}</td>
            </tr>
            <tr>
                <th>DateA</th>
                <td>{{ abonnement.dateA ? abonnement.dateA|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateExpA</th>
                <td>{{ abonnement.dateExpA ? abonnement.dateExpA|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EtudiantA</th>
                <td>{{ abonnement.etudiantA }}</td>
            </tr>
            <tr>
                <th>RedEtA</th>
                <td>{{ abonnement.redEtA }}</td>
            </tr>
        </tbody>
    </table>

 {{ include('abonnement/_delete_form.html.twig') }}
{% endblock %}
