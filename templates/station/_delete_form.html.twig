<form method="post" action="{{ path('app_station_delete', {'idstation': station.idstation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ station.idstation) }}">
    <button class="btn">Delete</button>
</form>
