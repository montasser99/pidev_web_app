<form method="post" action="{{ path('app_circuit_delete', {'idcircuit': circuit.idcircuit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ circuit.idcircuit) }}">
     {{ include('circuit/_delete_form.html.twig') }}
</form>
