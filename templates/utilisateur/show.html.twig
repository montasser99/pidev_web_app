{% extends 'base-back.html.twig' %}
{% block contenu %}
{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
              <!--<th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>-->
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
            <!-- <th>Roles</th>
                <td>{{ utilisateur.roles ? utilisateur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ utilisateur.password }}</td>
            </tr>-->
            <tr>
                <th>Nom:</th>
                <td>{{ utilisateur.nomu }}</td>
            </tr>
            <tr>
                <th>Prenom:</th>
                <td>{{ utilisateur.prenomu }}</td>
            </tr>
            <tr>
                <th>Telephone:</th>
                <td>{{ utilisateur.telephoneu }}</td>
            </tr>
            <tr>
                <th>Role:</th>
                <td>{{ utilisateur.roleu }}</td>
            </tr>
            <tr>
                <th>Cin:</th>
                <td>{{ utilisateur.cinu }}</td>
            </tr>
            <tr>
                <th>Imageurl:</th>
                <td>{{ utilisateur.imagepu }}</td>
            </tr>
            <tr>
                <th>Abonnement:</th>
                <td>{{ utilisateur.abonneu ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('app_utilisateur_index') }}">back to list</a>


    {{ include('utilisateur/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
