<!DOCTYPE html>
<html>
<head>
	<title>Etat journaliere</title>
	<style>
		body {
			font-family: Arial, sans-serif;
			background-color: #F0F0F0;
			margin: 0;
			padding: 0;
		}
		h1 {
			text-align: center;
			color: #1E90FF;
			margin-top: 30px;
			margin-bottom: 30px;
		}
		table {
			border-collapse: collapse;
			width: 50%;
			margin: 0 auto;
			background-color: #FFFFFF;
			box-shadow: 0px 0px 10px #888888;
		}
		th {
			background-color: #1E90FF;
			color: #FFFFFF;
			font-size: 18px;
			font-weight: bold;
			padding: 15px;
			text-align: left;
			border-bottom: 2px solid #ddd;
		}
		td {
			background-color: #F0F8FF;
			color: black;
			font-size: 16px;
			padding: 10px;
			border-bottom: 1px solid #ddd;
		}
		.transport {
            
			font-size: 20px;
			margin-top: 10px;
			margin-bottom: 10px;
		}
		.price {
            color:green;
			font-size: 20px;
			margin-top: 20px;
			margin-bottom: 20px;
            
		}
		img {
            display:block;
            margin: 80px auto;
            max-width: 20px%;
            width: 210px;
            height: 160px;
            margin-left: 50px;
            margin-top: 107px;
            margin-bottom: -160px;
		}
	</style>
</head>
<body>	<h1>Etat journalière</h1>
<div style="text-align:center; margin-bottom:-200px;">Nom de l'administrateur :
<span style="color:red"> Montasser benouirane</span></div>
{% for etatJ in etatj %}
	
		
						{% if etatJ.type =='Bus' %}
	<img src="{{ absolute_url(asset('uploads/images/bus.png')) }}" alt="Image de bus">
						{% elseif etatJ.type =='Metro' %}
	<img src="{{ absolute_url(asset('uploads/images/metro.jpg')) }}" alt="Image de metro">
						{% else %}
	<img src="{{ absolute_url(asset('uploads/images/train.jpg')) }}" alt="Image de Train">
						{% endif %}
		
		<table style="margin-right:100px; ">
			<tbody>
			
              
			
		
				<tr>
					<th>Date de réservation</th>
					<td style="font-size: 20px;">{{ etatJ.datereservation.date|date('Y-m-d') }}</td>
				</tr>
				<tr>
					<th>Moyen de transport</th>
					<td class="transport">{{ etatJ.type }} {{ etatJ.numerot }}</td>
				</tr>
				<tr>
					<th>Prix total</th>
					<td class="price">{{ etatJ.sum }}</td>
				</tr>
			</tbody>
		</table>
	{% endfor %}
	<h4 style="text-align:center">Signature electronique</h4>
<img  src="{{ absolute_url(asset(file)) }}" style="margin-left: 215px; margin-top:5px;" alt="Signature">
</body>
</html>
