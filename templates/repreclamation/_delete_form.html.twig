<form method="post" action="{{ path('app_repreclamation_delete', {'idrep': repreclamation.idrep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repreclamation.idrep) }}">
    <button class="btn">Delete</button>
</form>
