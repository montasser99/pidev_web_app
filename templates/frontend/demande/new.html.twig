{% extends 'frontend/index.html.twig' %}

{% block title %}New Demande{% endblock %}

{% block body %}
   <div class="row">
		<div class="col-md-6" style="margin-left: 270px;">

			<div class="panel panel-default" style="box-shadow: 0 0 30px wight;">

				
					<h4>
						<strong> Demande</strong>
					</h4>
				</div>
    {% for type, messages in app.flashes() %}
    {% for message in messages %}
        <div class="alert alert-{{ type }}" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

    {{ include('frontend/demande/_form.html.twig') }}

   
{% endblock %}
